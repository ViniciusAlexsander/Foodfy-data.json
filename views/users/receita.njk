{% extends "users/layout.njk" %}
{% block head %}
  <title>Foodfy -
    {{receita.title}}</title>
  <link rel="stylesheet" href="styles.css"/>
{% endblock %}
{% block content %}
  <div class="receita">
    <img src="{{receita.image}}"/>
    <h2>{{receita.title}}</h2>
    <p>por
      {{receita.author}}</>
    <div class="ingredientes topicos">
      <div class="subtitulos">
        <h3>Ingredientes</h3>
        <button class="esconder">Esconder</button>
      </div>
      <ul>
        {% for ingrediente in receita.ingredients %}
          <li>{{ingrediente}}</li>
          {% endfor%}
        </ul>
      </div>
    </div>
    <div class="preparo topicos">
      <div class="subtitulos">
        <h3>Modo de preparo</h3>
        <button class="esconder" value="mostrar">Esconder</button>
      </div>
      <ul>
        {% for passo in receita.preparation %}
          <li>{{passo}}
          </li>
          {% endfor%}
        </ul>
      </div>
      <div class="adicionais topicos">
        <div class="subtitulos">
          <h3>Informações adicionais</h3>
          <button class="esconder">Esconder</button>
        </div>
        <p>{{receita.information}}</p>
      </div>
    </div>
    {% endblock%}